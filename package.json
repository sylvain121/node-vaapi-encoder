{
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "test": "mocha",
    "install": "node-gyp rebuild"
  },
  "devDependencies": {
    "nan": "^2.2.0",
    "mocha": "^3.1.2",
    "node-pre-gyp": "^0.6.19"
  },
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.4.0"
  },
  "gypfile": true,
  "name": "node-vaapi-encoder",
  "description": "node module to create h264 frame from RGB bitmap with vaapi for intel and radeonsi",
  "version": "1.0.0",
  "directories": {
    "test": "tests"
  }
}
